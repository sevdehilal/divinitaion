import 'package:flutter/material.dart';

class FortuneTellerRegisterPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Falcı Olarak Kayıt Ol'),
      ),
      body: Center(
        child: Text('Falcı olarak kayıt ol sayfası'),
      ),
    );
  }
}
