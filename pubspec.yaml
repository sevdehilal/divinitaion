name: divinitaion
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  image_picker: ^0.8.7+3
  file_picker: ^5.3.0


flutter:
  uses-material-design: true

  # Assets bölümünü aktif hale getiriyoruz.
  assets:
    - lib/assets/logo1.png 
    - lib/assets/kahvefali.png 
    - lib/assets/tarot.png
    - lib/assets/hand.png 
    - lib/assets/dharita.png # Burada logonuzun doğru yolunu eklediğinizden emin olun.
